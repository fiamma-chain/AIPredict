# Trading Platform Configuration
ENABLED_PLATFORMS=hyperliquid,aster  # Enabled platforms, comma separated (Alpha/Beta groups trade on both platforms, individual AIs only trade on Aster)

# Hyperliquid Configuration (Mainnet by default)
HYPERLIQUID_TESTNET=False
HYPERLIQUID_API_URL=https://api.hyperliquid.xyz

# Aster Configuration (Mainnet only)
ASTER_TESTNET=False
ASTER_API_URL=https://fapi.asterdex.com

# API Configuration
API_HOST=0.0.0.0
API_PORT=46000

# Allowed Trading Symbols
ALLOWED_TRADING_SYMBOLS=BTC

# AI API Keys
CLAUDE_API_KEY=your_claude_api_key
OPENAI_API_KEY=your_openai_api_key
GPT_MODEL=gpt-4o
GEMINI_API_KEY=your_gemini_api_key
QWEN_API_KEY=your_qwen_api_key
QWEN_USE_INTERNATIONAL=true  # true=Alibaba Cloud International, false=Alibaba Cloud China
GROK_API_KEY=your_grok_api_key
DEEPSEEK_API_KEY=your_deepseek_api_key

# AI Trading Configuration (Dynamic Leverage Strategy)
AI_INITIAL_BALANCE=240.0           # Initial account balance
AI_MIN_MARGIN=200.0                # Minimum margin (USD)
AI_MAX_MARGIN=500.0                # Maximum margin (USD)
AI_MIN_LEVERAGE=5.0                # Minimum leverage (AI dynamically adjusts based on confidence)
AI_MAX_LEVERAGE=20.0               # Maximum leverage (AI dynamically adjusts from 5-20x)
AI_STOP_LOSS_PCT=0.02              # Stop loss percentage (2% = 0.02) short-term strategy
AI_TAKE_PROFIT_PCT=0.03            # Take profit percentage (3% = 0.03) short-term strategy
#
# Leverage calculation formula: leverage = min_leverage + ((confidence - 50) / 50) × (max_leverage - min_leverage)
#   Confidence 50% → 5x leverage, Confidence 75% → 12.5x leverage, Confidence 100% → 20x leverage
# Margin calculation formula: margin = min_margin + ((confidence - 50) / 50) × (max_margin - min_margin)
#   Confidence 50% → $200 margin, Confidence 75% → $350 margin, Confidence 100% → $500 margin
# Position value = margin × leverage
#
# Stop loss/Take profit strategy (short-term trading):
#   Stop loss: -2% (immediate stop loss when price drops 2%)
#   Take profit: +3% (immediate take profit when price rises 3%)
#   Risk-reward ratio: 1:1.5
#
# ⚠️ Important notice: 2% stop loss requires attention to liquidation risk at high leverage
#   - 5x leverage: -2% stop loss effective (liquidation at -20%) ✅
#   - 10x leverage: -2% stop loss effective (liquidation at -10%) ✅
#   - 20x leverage: -2% stop loss effective (liquidation at -5%) but close to liquidation ⚠️
#
# Examples (minimum balance $500 to support maximum position):
#   - Confidence 50%: Leverage  5.0x, Margin $200, Position $1,000, Stop loss -2%, Take profit +3%
#   - Confidence 75%: Leverage 12.5x, Margin $350, Position $4,375, Stop loss -2%, Take profit +3%
#   - Confidence 100%: Leverage 20.0x, Margin $500, Position $10,000, Stop loss -2%, Take profit +3%

# Alpha Group Configuration (DeepSeek + Claude + Grok)
GROUP_1_NAME=Alpha Group
GROUP_1_PRIVATE_KEY=your_private_key_for_alpha_group

# Beta Group Configuration (GPT-4 + Gemini + Qwen)
GROUP_2_NAME=Beta Group
GROUP_2_PRIVATE_KEY=your_private_key_for_beta_group

# Consensus Configuration
CONSENSUS_MIN_VOTES=2
CONSENSUS_INTERVAL=300  # Decision interval (seconds)
MIN_CONFIDENCE=60.0  # Minimum confidence threshold

# Multi-platform Comparison Mode
MULTI_PLATFORM_MODE=True
PLATFORM_COMPARISON_ENABLED=True

# Individual AI Trader Private Key Configuration (Optional, for AI Arena mode)
# If a private key is configured, the AI will start as an independent trader (no consensus required, independent trading decisions)
# Leave empty to disable this AI as an independent trader
# Note: Each individual AI needs a different private key and account, ensure the account has sufficient initial funds
INDIVIDUAL_DEEPSEEK_PRIVATE_KEY=
INDIVIDUAL_CLAUDE_PRIVATE_KEY=
INDIVIDUAL_GROK_PRIVATE_KEY=
INDIVIDUAL_GPT_PRIVATE_KEY=
INDIVIDUAL_GEMINI_PRIVATE_KEY=
INDIVIDUAL_QWEN_PRIVATE_KEY=

