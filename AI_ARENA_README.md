# 🤖 AI Trading Arena - AI 模型竞技场

让不同的 AI 模型（Claude, GPT-4, Gemini 等）在 Hyperliquid 上真实交易并竞争！

## 🎯 核心理念

这个项目与 [nof1.ai](https://nof1.ai) 类似，让真实的 AI 模型：
- 📊 分析市场数据
- 🧠 做出交易决策
- 💰 在 Hyperliquid 上执行合约交易
- 📈 比较每个 AI 的收益表现

## 🚀 快速开始

### 1. 安装依赖

```bash
pip install -r requirements.txt
```

### 2. 配置环境变量

在 `.env` 文件中配置：

```bash
# Hyperliquid 配置
HYPERLIQUID_PRIVATE_KEY=你的私钥
HYPERLIQUID_TESTNET=true

# AI API 密钥（至少配置一个）
CLAUDE_API_KEY=sk-ant-xxx
OPENAI_API_KEY=sk-xxx
GEMINI_API_KEY=xxx

# AI 交易配置
AI_INITIAL_BALANCE=1000
AI_MAX_POSITION_SIZE=200
ARENA_UPDATE_INTERVAL=300
```

### 3. 运行 AI 竞技场

```bash
python3 ai_arena_main.py
```

## 📋 获取 API 密钥

### Claude API (Anthropic)
1. 访问 https://console.anthropic.com/
2. 注册账号并创建 API Key
3. 将密钥设置为 `CLAUDE_API_KEY`

### GPT API (OpenAI)
1. 访问 https://platform.openai.com/api-keys
2. 创建新的 API Key
3. 将密钥设置为 `OPENAI_API_KEY`

### Gemini API (Google)
1. 访问 https://ai.google.dev/
2. 获取 API Key
3. 将密钥设置为 `GEMINI_API_KEY`

## 🏟️ AI 竞技场工作原理

### 1. 数据收集
系统从 Hyperliquid 获取实时市场数据：
- 当前价格
- 24小时成交量
- 资金费率
- 订单簿深度
- 未平仓合约

### 2. AI 分析
每个 AI 模型收到相同的市场数据，并独立分析：
```
市场数据 → AI 模型 → 交易决策 + 置信度 + 理由
```

### 3. 决策执行
根据 AI 的决策和置信度执行交易：
- **STRONG_BUY** / **BUY** → 开多仓
- **SELL** / **STRONG_SELL** → 平仓
- **HOLD** → 不操作

### 4. 性能追踪
独立追踪每个 AI 的表现：
- 总盈亏
- ROI%
- 胜率
- 交易次数
- 权益曲线

## 📊 AI 模型对比

| AI 模型 | 优势 | 适用场景 |
|---------|------|----------|
| **Claude** | 逻辑推理强，风险控制好 | 稳健型交易 |
| **GPT-4** | 模式识别优秀 | 趋势捕捉 |
| **Gemini** | 反应快速 | 短期波动 |

## 💡 工作流程

```
┌─────────────────────────────────────────────────────────┐
│  1. 每 5 分钟触发一次竞技场周期                          │
└────────────────────┬────────────────────────────────────┘
                     │
        ┌────────────┴────────────┐
        │  获取市场数据            │
        │  - BTC, ETH, SOL         │
        │  - 价格、订单簿、成交量   │
        └────────────┬────────────┘
                     │
        ┌────────────┴────────────┐
        │  并行调用所有 AI         │
        │  - Claude 分析           │
        │  - GPT-4 分析            │
        │  - Gemini 分析           │
        └────────────┬────────────┘
                     │
        ┌────────────┴────────────┐
        │  获取 AI 决策            │
        │  - 买/卖/持有           │
        │  - 置信度 (0-100)       │
        │  - 决策理由             │
        └────────────┬────────────┘
                     │
        ┌────────────┴────────────┐
        │  风险检查                │
        │  - 仓位限制             │
        │  - 亏损限制             │
        │  - 最小余额             │
        └────────────┬────────────┘
                     │
        ┌────────────┴────────────┐
        │  执行交易                │
        │  - 在 Hyperliquid 下单  │
        │  - 更新 AI 虚拟账户     │
        └────────────┬────────────┘
                     │
        ┌────────────┴────────────┐
        │  更新排行榜              │
        │  - 计算盈亏             │
        │  - 更新权益曲线         │
        │  - 显示排名             │
        └─────────────────────────┘
```

## 📈 示例输出

```
================================================================================
🏆 AI 模型排行榜
================================================================================
排名   模型                  余额             盈亏             ROI        交易数   胜率    
--------------------------------------------------------------------------------
1      Claude (Sonnet)      $1,234.56       $234.56         23.46%     12       66.7%   
2      GPT-4 (Turbo)        $1,156.78       $156.78         15.68%     15       60.0%   
3      Gemini (Pro)         $1,089.23       $89.23          8.92%      18       55.6%   
================================================================================
```

## ⚙️ 配置参数

### AI 配置
- `AI_INITIAL_BALANCE`: 每个 AI 的起始资金 (默认: $1000)
- `AI_MAX_POSITION_SIZE`: 单笔最大仓位 (默认: $200)
- `ARENA_UPDATE_INTERVAL`: 更新间隔秒数 (默认: 300)

### 风险管理
- `MAX_POSITION_SIZE`: 最大仓位限制
- `DAILY_LOSS_LIMIT`: 每日亏损上限
- `MAX_OPEN_POSITIONS`: 最大持仓数

## 🔒 安全提示

1. **从测试网开始** - 始终在测试网验证
2. **小额测试** - 从小金额开始
3. **监控 API 使用** - 注意 AI API 调用成本
4. **设置止损** - 使用风险管理参数
5. **定期检查** - 监控 AI 的表现和决策

## 📊 API 接口

竞技场提供 RESTful API:

```bash
# 获取所有 AI 模型
GET http://localhost:8000/ai-models

# 获取排行榜
GET http://localhost:8000/leaderboard

# 获取性能对比
GET http://localhost:8000/performance/comparison

# 获取单个 AI 详情
GET http://localhost:8000/ai-models/{model_name}
```

## 🎨 Web 界面

打开 `web/index.html` 查看可视化界面：
- 实时排行榜
- 权益曲线对比图
- AI 决策历史
- 性能指标对比

## 💰 成本估算

### API 调用成本（每天）
假设 3 个 AI，3 个交易对，5分钟更新一次：

```
每小时: 12 次 × 3 个 AI × 3 个币种 = 108 次调用
每天: 108 × 24 = 2,592 次调用

Claude: ~$0.003/调用 = ~$7.78/天
GPT-4: ~$0.01/调用 = ~$25.92/天
Gemini: ~$0.001/调用 = ~$2.59/天
```

**建议**: 增加更新间隔以降低成本

## 🚫 限制和注意事项

1. **虚拟资金池** - 每个 AI 有独立的虚拟账户，但共享同一个 Hyperliquid 实际账户
2. **API 限制** - 注意各 AI 提供商的速率限制
3. **市场波动** - AI 可能在极端市场条件下表现异常
4. **延迟** - API 调用和网络延迟可能影响执行
5. **幻觉问题** - AI 可能产生不合理的决策

## 🛠️ 故障排除

### 问题: AI 不交易
- 检查 API 密钥是否正确
- 查看日志了解 AI 的决策
- 确认账户余额充足

### 问题: API 调用失败
- 验证 API 密钥有效性
- 检查网络连接
- 确认 API 配额未超限

### 问题: 交易未执行
- 检查风险管理限制
- 确认 Hyperliquid 连接正常
- 查看订单管理器日志

## 📚 进阶功能

- [ ] 添加更多 AI 模型 (Mistral, Llama, etc.)
- [ ] 实现投票机制（多个 AI 共同决策）
- [ ] 添加回测功能
- [ ] 实现策略参数优化
- [ ] 添加更多技术指标作为输入

## 🤝 贡献

欢迎提交 PR 添加新的 AI 模型或改进现有功能！

## ⚖️ 免责声明

本项目仅供教育和研究目的。加密货币交易存在风险，可能导致资金损失。使用本软件进行交易的所有风险由用户自行承担。作者不对任何损失负责。

---

**Happy Trading! 🚀📈💰**

